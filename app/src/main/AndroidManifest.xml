<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.smarfirahdiskusi6">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SMarfirahDiskusi6">
        <activity android:name=".shape_cell"
            tools:ignore="Instantiatable"></activity>
        <activity android:name=".activity_listview"
            tools:ignore="Instantiatable" />
        <activity android:name=".activity_list_detail" />
        <activity android:name=".activity_hitung_vokal"
            tools:ignore="Instantiatable" />
        <activity android:name=".activity_alert"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>